{
  "scripts": {
    "postinstall": "npm run bootstrap",
    "setup": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "test-cvr": "lerna run test-cvr",
    "test-all-cvr": "lerna run test-all-cvr",
    "cleanup": "lerna run cleanup",
    "aftertests": "lerna run aftertests",
    "cix-lic": "lerna run cix-lic",
    "coverage": "lerna run coverage",
    "release": "lerna publish --force-publish --exact",
    "release-patch": "lerna publish --force-publish --exact --cd-version=patch --yes",
    "release-next": "lerna publish --force-publish --exact --cd-version=prerelease --npm-tag=next --yes"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
