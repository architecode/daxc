{
  "scripts": {
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "test-cvr": "lerna run test-cvr",
    "test-all-cvr": "lerna run test-all-cvr",
    "cleanup": "lerna run cleanup",
    "aftertests": "lerna run aftertests",
    "cix-lic": "lerna run cix-lic",
    "coverage": "lerna run coverage",
    "publish": "lerna publish",
    "publish-patch": "lerna publish --cd-version=patch --yes",
    "publish-next": "lerna publish --cd-version=prerelease --npm-tag=next --yes"
  },
  "devDependencies": {
    "lerna": "^3.4.0"
  }
}
